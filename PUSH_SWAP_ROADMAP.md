# PUSH_SWAP VISUAL ROADMAP 🗺️

```
┌─────────────────────────────────────────────────────────────┐
│                    ./push_swap 8 2 6 1 9 4                 │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                      MAIN.C                                 │
│  • Initialize s_numbers, stack_a, stack_b                  │
│  • Check if ac == 1 or single word                         │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                   VALIDATE_ARGS()                          │
│  • validate_chars() - only digits/signs/spaces             │
│  • count_total_nums() - count all numbers                  │
│  • malloc array for numbers                                │
│  • ft_split_atoi() for multi-word args                     │
│  • ft_atoi() for single numbers                            │
│  • ft_any_double() - check duplicates                      │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                  BUILD STACK_A                             │
│  • Loop through s_numbers.numbers[]                        │
│  • lstnew() for each number                                │
│  • lst_add_back() to build stack                           │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                     SORT()                                 │
│  if (count == 2) → sort_2()                                │
│  if (count == 3) → sort_3()                                │
│  if (count == 4) → sort_4()                                │
│  if (count == 5) → sort_5()                                │
│  if (count > 5)  → turk()                                  │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                   TURK() - 5 PHASES                        │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│  PHASE 1: INITIAL PUSH                                     │
│  • pb() - push first element A→B                           │
│  • pb() - push second element A→B                          │
│                                                             │
│  A: [6,1,9,4]    B: [2,8]                                  │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│  PHASE 2: PUSH CHEAPEST (while stack_size(A) > 3)         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  find_cheapest(A, B)                                │   │
│  │  • Loop through all elements in A                   │   │
│  │  • For each element:                                │   │
│  │    - idx_a = get_index(A, value)                    │   │
│  │    - idx_b = find_target_position(B, value)         │   │
│  │    - cost = calc_total_cost(idx_a, idx_b, size_a, size_b) │
│  │  • Return element with minimum cost                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                │                            │
│                                ▼                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  do_rotations(A, B, idx_a, idx_b)                  │   │
│  │  • get_dir_cost() for A and B                       │   │
│  │  • If same direction: use rr/rrr optimization       │   │
│  │  • Rotate A: ra/rra until element at TOP            │   │
│  │  • Rotate B: rb/rrb until target at TOP             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                │                            │
│                                ▼                            │
│  • pb() - push cheapest element A→B                        │
│                                                             │
│  A: [1,9,4]    B: [6,2,8]                                  │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│  PHASE 3: SORT REMAINING 3 IN A                            │
│  • sort_3(A) - handle all 6 cases                          │
│                                                             │
│  A: [1,4,9]    B: [6,2,8]                                  │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│  PHASE 4: PUSH BACK B→A (while stack_size(B) > 0)         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  For each element in B:                             │   │
│  │  • idx_a = find_target_in_a(A, B_top_value)        │   │
│  │  • Rotate A until target position at TOP            │   │
│  │    - if idx_a <= size_a/2: ra                       │   │
│  │    - else: rra                                       │   │
│  │  • pa() - push B→A                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  A: [8,9,1,2,4,6]    B: []                                 │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│  PHASE 5: FINAL ROTATION                                   │
│  • idx_a = get_index(A, find_min(A))                       │
│  • Rotate A until MIN at TOP                               │
│    - if idx_a <= size_a/2: ra                              │
│    - else: rra                                              │
│                                                             │
│  A: [1,2,4,6,8,9] ✅ SORTED!    B: []                      │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    OUTPUT OPERATIONS                       │
│  pb, pb, pb, sa, ra, rra, pa, rra, pa, rra, rra, pa, ra, ra │
│                     (14 operations)                        │
└─────────────────────────────────────────────────────────────┘
```

## KEY HELPER FUNCTIONS:
- `get_dir_cost(idx, size, *dir, *cost)` → direction and cost calculation
- `calc_total_cost(idx_a, idx_b, size_a, size_b)` → optimized total cost with rr/rrr
- `find_target_position(B, value)` → where to place in B
- `find_target_in_a(A, value)` → where to place in A
- `stack_size()`, `find_min()`, `find_max()`, `get_index()`

## PERFORMANCE:
- 100 numbers: ~651 operations (< 700) ✅
- 500 numbers: ~5022 operations (< 5500) ✅

## OPTIMIZATIONS USED:
- `rr` = `ra` + `rb` in one operation (same direction up)
- `rrr` = `rra` + `rrb` in one operation (same direction down)
- Smart cost calculation with `calc_total_cost()` considering rr/rrr optimization
- Turk algorithm for optimal element selection
